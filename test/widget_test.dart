// 占位 smoke 测试 —— 桌面便签应用初始化阶段。
// 由于应用启动需要 Hive / window_manager / tray_manager 等桌面环境，
// 这里仅做一个最小可编译的占位测试。完整的 widget 测试请在桌面端运行。

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
